# D1 release and live verification

Release `profiles-818f418a` is deployed on DRISHTI and EQA. It contains the profile-wire master, the shared captured-type precision fix and custom command 66 support. The deployed Gurux BER fix remains in place. The Release suite passes 592 tests; nine composer checks pass. The [manifest](../../deploy/profiles-20260914-manifest.json) records exact source, DLL and XML hashes, full backup locations and both targets' preserved saved state.

DRISHTI PID 20000 started at 19:30:34 UTC; EQA PID 4210 started at 19:32:38 UTC. Both have zero restarts and passed HTTP, IPv6 listener/route and saved logical state checks. EQA's first follow-up audit ran while its final batch was still recovering; that batch completed at 19:34:30 and the unchanged-state audit then passed. No second restart was required. EQA has approximately 298 MiB root space remaining. Its complete app/data recovery archive was downloaded and hash-verified before application replacement.

The deployed master preserves 160 object definitions. It includes 100 fresh Block rows, three Daily rows, one Instant row and 39 event rows in four families, using the captured wire values rather than Gurux's scaled client values. The original compatible 13-row Billing donor remains. The physical Billing capture references class 4 at `1.0.84.6.0.255`; its association instead declares class 3, and a class-4 scaler read returns undefined object. The base has no standalone definition for that LN. No object or scaler was invented to bypass the compatibility check.

## Live commands during EQA's deployment window

| Check | Evidence | Outcome |
| --- | --- | --- |
| DLMS Instant | 98932983, frame 10004, completed 19:32:22.667849 UTC, one try. Passive capture has exactly seven requests and seven responses, all before EQA restarted. | Public/counter/release/secure association/profile/release exchange completed. HES marked success, but rejected the profile row: import kWh 4684834.96 and kVAh 4883629.882 from the batch's older selected XML failed energy validation. No Instant row was persisted. |
| Custom ESW | 98932984, frame 10002, completed 19:32:45.125703 UTC, one try. Persisted ESW row 3908521 has RTC 19:32:45 and creation 19:32:45.113609 UTC. | All 128 JSON-response and stored bits equal the deployed PKG9 ESW-1, and differ from its ESWF filter. This command completed before EQA's RF batch recovered. |

The temporary isolation came from the already authorized EQA deployment; no batch was paused or moved for these tests. Existing template selections remain preserved: deploying D1_Master.xml does not automatically assign it to a batch. The corrected master must be selected for the intended isolated D1 test batch before repeating Instant persistence.

At 19:26:26 UTC the HES pull service was active, while push variants and routing were inactive. Ongoing fleet ownership is still awaiting the user's preference because both MAYA hosts answer overlapping node IDs after recovery. No HES service, configuration, routing row or schema was changed. Other D1 paths, GRBlockLoad, normal-topic push validation and future D2/D3 waves remain incomplete.

Complete source snapshot, capture reports, raw profile values, live packet observation and bit-by-bit verification are retained under `C:\Users\ayush\Documents\MAYA-release-receipts\profiles-818f418a`.

## Local HES Common timestamp correction

The isolated Common worktree at base `0337feb506a1c9963ffd42d50c3aa592327c10dc` now preserves zero-deviation UTC datetimes and converts DLMS hundredths to milliseconds. The actual compiled Common probe passes 28 compatibility cases, including four previously failing full-UTC expectations from recorded push timestamps and a synthetic fractional timestamp. Nonzero and unspecified deviations retain the previous -330-minute fallback; this is a compatibility boundary, not a resolved general deviation policy. The verified assembly hash is `e71c67ad58eb432118e3e8386f42c1f1762e444798823692512379b633adf764`. These Common edits have not been released or deployed, so live HES timestamp behavior is not yet corrected.

Source inspection also explains why protocol completion alone is insufficient evidence of ingestion: Common's `ParseDLMS` returns void, and the single-phase Instant path logs a failed repository add without returning its failure to the caller. This observation accompanies command 98932983's successful command status and absent profile row. No persistence-result contract change has been made.
